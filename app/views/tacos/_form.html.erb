<%= form_with(model: taco, local: true) do |form| %>
  <% if taco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taco.errors.count, "error") %> prohibited this taco from being saved:</h2>

      <ul>
      <% taco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :tortilla %>
    <%= form.text_field :tortilla %>
  </div>

  <div class="field">
    <%= form.label :cheese %>
    <%= form.check_box :cheese %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
